
## M5STICKPLUS2 PARA COMPRA TRADE DE BITCOIN

## 🔐 Configuração de Credenciais

As credenciais do Wi-Fi ficam armazenadas no arquivo `include/secrets.h`. **Nunca suba esse arquivo com os dados reais para o GitHub!**

Exemplo do conteúdo de `secrets.h`:

```cpp
#define WIFI_SSID "SEU_SSID"
#define WIFI_PASSWORD "SUA_SENHA"

⚙️ Plataforma e Dependências
Plataforma: M5StickCPlus2

Ambiente: PlatformIO no VS Code

Bibliotecas utilizadas:

M5StickCPlus2 (M5Unified)

WiFi

HTTPClient

ArduinoJson

🚀 Configuração do Projeto
Clone o repositório:

bash
Copiar
Editar
git clone https://github.com/danielfmoraes/btc-monitor-m5stickplus.git
Configure as credenciais do Wi-Fi:

Edite o arquivo include/secrets.h com seu SSID e senha.

Abra o projeto no VS Code e use o PlatformIO para compilar e enviar o firmware para o dispositivo.

📡 Funcionamento
Atualização dos Dados:
A cada 30 segundos, o dispositivo busca os dados do preço do Bitcoin utilizando a API do CoinGecko (do endpoint /coins/markets), exibindo:

Preço atual em USD e BRL.

Variação percentual de 1 hora e 24 horas.

Registro de Compra:
Ao pressionar o botão A:

O preço atual (USD) é registrado como preço de compra.

Uma mensagem de confirmação "Comprado: $<valor>" é exibida.

A partir daí, o display mostra a variação percentual do preço atual em relação ao preço de compra, com a cor indicando:

Verde: Alta.

Vermelho: Queda.

Reset:
O registro da compra é mantido até o dispositivo ser reiniciado.
